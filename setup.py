from setuptools import find_packages, setup


setup(
    name='rompy',
    version=0.1,
    description='Python package for reduced-order modeling applications',
    url='https://bitbucket.org/chadgalley/rompy.git',
    packages=find_packages(),
)
